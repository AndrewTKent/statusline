# Codex CLI Statusline Configuration
# Copy to ~/.codex/statusline.conf

# Keep the full Claude Code-style dashboard below the composer. Set to 1 for
# Codex's compact native footer instead.
CODEX_STATUSLINE_NATIVE=0

# Render format: default | sigil
# CODEX_STATUSLINE_FORMAT=default

# Live dashboard refresh interval used by bin/codex-watch.
# CODEX_WATCH_INTERVAL=2

# Top monitor refresh interval used by bin/codex-top.
# CODEX_TOP_INTERVAL=2

# Claude Code-style footer height, refresh interval, and scrollback depth.
# The default renderer occupies 11 fixed rows.
# CODEX_STATUSLINE_HEIGHT=11
# CODEX_STATUSLINE_INTERVAL=1
# CODEX_STATUSLINE_HISTORY_LIMIT=100000

# The launcher defaults to --dangerously-bypass-approvals-and-sandbox. Set this to 0
# to inherit Codex permission settings instead; explicit CLI permission flags win.
# CODEX_STATUSLINE_MANAGE_APPROVALS=1

# Pin the footer to one thread instead of process binding.
# CODEX_STATUSLINE_THREAD_ID=019f0000-0000-7000-8000-000000000000

# Codex binary for the launcher when `codex` is not on PATH.
# CODEX_STATUSLINE_CODEX_BIN=/usr/local/bin/codex

# Optional aggregate token goals, exposed via `codex-statusline --json`.
# SESSION_TOKEN_GOAL=1000000
# DAILY_TOKEN_GOAL=2000000
# WEEKLY_TOKEN_GOAL=10000000
# LIFETIME_TOKEN_GOAL=100000000

# Optional: choose a thread explicitly when running outside Codex.
# CODEX_THREAD_ID=019f0000-0000-7000-8000-000000000000

# Age past which a remote board's Codex rows render dimmed with their age
# rather than as current. The rows come from ~/.accounts/remote, pulled by
# `accounts poll`; see REMOTE_ACCOUNT_BOARDS in statusline.conf.
# REMOTE_BOARD_MAX_AGE=900
